services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: c2c
      POSTGRES_PASSWORD: c2c
      POSTGRES_DB: c2c
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
